# Optional publisher configuration options
loglevel: "info"
#logfile: "../test/onewire.log"
#publisherId: "onewire"

# Onewire config
#gatewayId: "gateway"

# OWServer gateway configuration
owserverAddress: 10.3.3.33
loginName: ""
password: ""
#gatewayID: "owserver:1"   // in case of multiple instances each gateway must have a unique Thing ID

# tdInterval in seconds for publishing updates to the TD's, default is 1 hour
tdInterval: 3600

# Interval to publish updates to property values, default is 60
valueInterval: 60
